import React from "react";
import BackButton from "../components/BackButton";

const CartPage = () => {
  return (
    <div style={{ padding: "40px", maxWidth: "800px", margin: "0 auto" }}>
      <BackButton />
      <h2>Your Cart</h2>
      <p>Cart page is under construction 😊</p>
    </div>
  );
};

export default CartPage;   // ✅ default export
